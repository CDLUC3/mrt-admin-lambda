The Merritt DEV boxes have been configured to support automated testing of *many* of the existing reports/actions.

Testing is only supported to the stage environment.

```
cd src-testdriver
bundle exec rspec
```